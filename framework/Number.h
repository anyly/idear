#ifndef NUMBER_H_INCLUDED
#define NUMBER_H_INCLUDED

#include "type.h"

typedef int Int;

typedef double Double;

typedef long Long;

typedef float Float;

typedef unsigned char Byte;

typedef bit Bit;

typedef bit Boolean;

#define true 1;

#define false 0;

Any addition(Identifier a, Identifier b);

Any subtraction(Identifier a, Identifier b);

Any multiplication(Identifier a, Identifier b);

Any division(Identifier a, Identifier b);


#endif // NUMBER_H_INCLUDED
